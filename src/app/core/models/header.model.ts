/**
 * Header model class
 *
 * @author Francisco Franco
 * @version %I%, %G%
 * @since 1.0
 */

export class Header {
    applicationVersion: number;
    currentTime: number; 
    messageId: number;
    channelId: number;
    sourceId: string;
    posId: string;
}